#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main()
{
	int n, k;
	cin >> n >> k;

	vector <string> s;

	for (int i = 0; i < n; i++)
	{
		string str;
		cin >> str;
		s.push_back(str);
	}

	int arr[50] = { 0, };

	
}